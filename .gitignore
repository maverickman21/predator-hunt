# secrets — NEVER commit
.env
.env.*
*.key
*.pem

# dependencies (package.json defines them)
node_modules/

# data — logged every minute, not source
*.csv

# runtime state — regenerated by the engine
regime_state.json
regime_history.json
regime_buffers.json
nq_price.json
eth_prices.json
macro_bias.json

# backups / archive
*.bak
*.bak2
*.pre-retention-fix
_archive/


sheets-key.json
sheets_cursor.json